import React from "react";

// crear un formulario que contenga los campos necesarios para registrar un nuevo usuario
// deberas crear un servicio que nos permita guardar la informacion

// crear un tipo al newUser
// manejar los errores
// manejar los tiempos de carga

// navegar a la pagina de /login una vez creado

export default function RegisterPage() {
  return <div>RegisterPage</div>;
}
